# Intentional-gap sample

`cloud-exit-evidence demo` writes this manifest and a partial offline copy to a new temporary directory. The missing photo and tax return, plus the Android exclusion, are intentional so the report shows the main audit outcomes.
